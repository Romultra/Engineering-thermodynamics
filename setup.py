# setup.py
from setuptools import setup, find_packages

setup(
    name='thermo',
    version='0.1',
    packages=find_packages(),
    install_requires=[],  # list dependencies here
    description='Tiny Thermodynamics Library',
    author='Romultra',
    author_email='romultra@gmail.com',
)
